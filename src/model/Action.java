package model;

/**
 * This enum encapsulates the values for the action to be done on the server.
 * 
 * @author Nicolas Rodriguez
 */
public enum Action {
    LOGIN,          // method User logIn(String username)
    REGISTER;   // method void signUp(User user)
}
